<style>
    main {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }
    main > span {
        text-align: center;
    }
    .connect-title {
        font-size: 2.5rem;
        text-align: center;
        word-break: break-all;
    }
    .connect-container {
        padding: 1rem;
        background-color: var(--secondary-bg);
        border-radius: 0.5rem;
        width: 100%;
        box-sizing: border-box;
        display: flex;
        gap: 0.3rem;
        flex-direction: column;
    }
    .connect-container ul {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-direction: row;
        flex-wrap: wrap;
    }
    .connect-container li {
        flex-basis: 14rem;
        /* flex-shrink: 0; */
        /* flex-grow: 1; */
    }
    .connect-container code {
        font-size: 0.6rem;
        padding: 0.05rem 0.15rem;
        border-radius: 0.5rem;
        background-color: var(--thirdendary-bg);
    }
    .connect-container span {
        overflow-wrap: break-word;
        font-size: 0.7rem;
    }
    .connect-image {
        border-radius: 0.5rem;
        background-color: green;
        background-position: center;
        background-repeat: no-repeat;
        background-size: cover;
        width: 100%;
        height: 8rem;
    }

    .connect-switcher {
        width: 100%;
        padding: 1rem;
        display: flex;
        justify-content: center;
        align-items: center;
        border-radius: 0.5rem;
        background-color: var(--primary);
        color: var(--text);
        transition: background-color 0.3s ease, color 0.3s ease;;
    }
    .connect-switcher:hover {
        background-color: var(--text);
        color: var(--primary);
    }
</style>