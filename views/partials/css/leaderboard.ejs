<style>
    .leaderboard {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }
    .country {
        width: 100%;
        padding: 0.8rem;
        border-radius: 0.5rem;
        background-color: var(--secondary-bg);
        position: relative;
    }
    .country:before {
        content: "";
        background-repeat: no-repeat;
        background-position: center;
        background-size: contain;
        background-image: url("/public/media/images/arrow-down.svg");
        position: absolute;
        top: 50%;
        right: 1.3rem;
        transform: translateY(-50%);
        height: 1rem;
        width: 1rem;
    }
    .country-select {
        width: 100%;
        padding: 0.4rem;
        padding-right: 1.6rem;
        font-size: 1rem;
        appearance: none;
        -webkit-appearance:none;
        font-family: 'Exo 2';
        display: inline-block;
        -webkit-box-sizing: border-box; /* Safari/Chrome, other WebKit */
        -moz-box-sizing: border-box;    /* Firefox, other Gecko */
        box-sizing: border-box;         /* Opera/IE 8+ */
        border-radius: 0.2rem;
        border: 0.2rem solid var(--primary-bg);
        background-color: var(--primary-bg);
        color: var(--text);
    }
    .country-select:focus {
        outline: none !important;
        -webkit-box-sizing: border-box; /* Safari/Chrome, other WebKit */
        -moz-box-sizing: border-box;    /* Firefox, other Gecko */
        box-sizing: border-box;         /* Opera/IE 8+ */
        border-radius: 0.2rem;
        border: 0.2rem solid var(--primary);
    }
    .options {
        padding: 0rem !important;
        background-color: unset !important;
        gap: 0.2rem;
        overflow: hidden;
        display: flex;
        flex-wrap: wrap;
        flex-direction: row;
    }
    .options-button {
        text-align: center;
        border-radius: 0.5rem;
        padding: 0.7rem 0.5rem;
        background-color: var(--primary);
        flex-basis: 6rem;
        flex-shrink: 0;
        flex-grow: 1;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
        transition: background-color 0.3s ease, color 0.3s ease;
    }
    .options-button:hover:not(.options-button-disabled) {
        cursor: pointer;
    }
    .options-button-active, .options-button:hover:not(.options-button-disabled) {
        background-color: var(--text);
        color: var(--primary);
    }
    .options-button-disabled {
        background-color: var(--thirdendary-bg);
    }
    .pagination-container {
        display: flex;
        justify-content: center;
        width: 100%;
        background-color: var(--secondary-bg);
        border-radius: 0.5rem;
        /* gap: 0.4rem; */
        padding: 0.6rem 0rem;
    }
    .pagination-container > * {
        border-radius: 0.5rem;
        padding: 0.2rem 0.4rem;
    }
    .pagination-active {
        background-color: var(--primary);
    }
    .player-table {
        --table-border-radius: 0.5rem;
        width: 100%;
        background-color: var(--secondary-bg);
        border-radius: var(--table-border-radius);
        padding: 0.8rem;
        padding-top: var(--table-border-radius);
        border-collapse: separate;
        border-spacing: 0rem 0.4rem;
    }
    .player-row {
        position: relative;
        transition: background-color 0.2s ease;
        background-color: var(--thirdendary-bg);
        animation-name: rowfadein;
        animation-duration: 0.1s;
    }
    @keyframes rowfadein {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }
    .player-row:hover {
        background-color: var(--primary-bg);
    }
    .player-table td {
        vertical-align: middle;
        text-align: center;
        width: 8%;
        font-size: 0.9rem;
        white-space: nowrap;
        padding: 0.7rem 0rem;
        border: solid 0 #000;
    }
    .player-table td:first-child { border-top-left-radius: var(--table-border-radius); border-bottom-left-radius: var(--table-border-radius); padding-left: 0.3rem; }
    .player-table td:last-child { border-top-right-radius: var(--table-border-radius); border-bottom-right-radius: var(--table-border-radius); padding-right: 0.3rem; }
    .player-country {
        padding: 0rem 0.3rem !important;
        width: 1.5rem !important;
    }
    .player-country-image {
        height: 1rem;
        width: 1.5rem;
        border-radius: 0.2rem;
        background-repeat: no-repeat;
        background-size: contain;
        background-position: center;
    }
    .player-table th {
        font-weight: normal;
        font-size: 0.7rem;
        padding: 0.3rem;
    }
    .player-rank {
        width: 1% !important;
    }
    .player-name {
        width: auto !important;
        text-align: left !important;
    }
    .player-row a:hover {
        text-decoration: underline;
    }

    @media(max-width: 600px) {
        .player-accuracy, .player-playcount {
            display: none
        }
    }
</style>