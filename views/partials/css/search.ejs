<style>
    .search-container {
        background-color: var(--secondary-bg);
        border-radius: 0.5rem;
        overflow: hidden;
        width: 100%;
    }
    .search-title {
        font-size: 2rem;
        background-color: var(--primary);
        padding: 0.8rem;
    }   
    .search-bar {
        background-color: var(--thirdendary-bg);
        padding: 0.8rem;
        position: relative;
    }
    .search-icon {
        position: absolute;
        background-image: url(/public/media/images/search.svg);
        background-size: contain;
        background-repeat: no-repeat;
        right: 1.3rem;
        top: 50%;
        transform: translateY(-50%);
        height: 1.2rem;
        width: 1.2rem;
    }
    .search-icon:hover {
        cursor: pointer;
    }
    .search-input {
        width: 100%;
        padding: 0.6rem;
        padding-right: 1.5rem;
        font-size: 1rem;
        appearance: none;
        -webkit-appearance:none;
        font-family: 'Exo 2';
        display: inline-block;
        -webkit-box-sizing: border-box; /* Safari/Chrome, other WebKit */
        -moz-box-sizing: border-box;    /* Firefox, other Gecko */
        box-sizing: border-box;         /* Opera/IE 8+ */
        border-radius: 0.2rem;
        border: 0.15rem solid var(--secondary-bg);
        background-color: var(--secondary-bg);
        color: var(--text);
    }
    .search-input:focus {
        outline: none !important;
        -webkit-box-sizing: border-box; /* Safari/Chrome, other WebKit */
        -moz-box-sizing: border-box;    /* Firefox, other Gecko */
        box-sizing: border-box;         /* Opera/IE 8+ */
        border-radius: 0.2rem;
        border: 0.15rem solid var(--primary);
    }
    .search-modes {
        display: flex;
        flex-wrap: wrap;
        border-image: linear-gradient(
        90deg,transparent,#fff,transparent) 1;
        border-bottom: 1px solid;
        border-top-width: 0;
        gap: 1rem;
        justify-content: center;
        padding: 0rem 0.8rem;
        font-weight: 600;
    }
    .search-modes-button {
        flex-shrink: 0;
        transition: box-shadow 0.3s ease;
        padding: 0.8rem 0rem;
    }
    .search-modes-button-active, .search-modes-button:hover:not(.search-modes-button-active) {
        box-shadow: inset 0rem -0.2rem 0rem 0rem var(--primary);
        cursor: pointer;
    }
    .search-modes-button-active:hover {
        cursor: pointer;
        box-shadow: inset 0rem -0.3rem 0rem 0rem var(--text);
    } 
    .search-modes span {
        font-size: 0.7rem;
        padding: 0.05rem 0.25rem;
        border-radius: 0.5rem;
        background-color: var(--primary-bg);
    }

    .search-pagination-container {
        font-size: 0.9rem;
        display: flex;
        justify-content: center;
        width: 100%;
        background-color: var(--secondary-bg);
        border-radius: 0.5rem;
        padding: 0.6rem 0rem;
    }
    .search-pagination-container > * {
        border-radius: 0.5rem;
        padding: 0.1rem 0.4rem;
    }
    .search-pagination-active {
        background-color: var(--primary);
    }
    .search-results {
        display: flex;
        justify-content: center;
        flex-wrap: wrap;
        gap: 0.5rem;
        padding: 0.8rem;
        flex-direction: row;
    }
    .search-results > * {
        display: flex;
        align-items: center;
        flex-direction: row;
        gap: 0.3rem;
        height: 6rem;
        border-radius: 0.5rem;
        flex-basis: 18rem;
        flex-grow: 0;
        padding: 0.8rem;
        background-color: var(--thirdendary-bg);
    }
    .results-users-information {
        display: flex;
        height: 3rem;
        flex-direction: column;
        justify-content: space-evenly;

    }
    .results-users-country {
        height: 1rem;
        width: 1.5rem;
        border-radius: 0.2rem;
        background-repeat: no-repeat;
        background-size: contain;
        background-position: center;
    }
    .results-users-icon {
        border-radius: 1rem;
        height: 4rem;
        width: 4rem;
    } 
</style>